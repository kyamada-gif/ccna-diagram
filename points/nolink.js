/* 「つながらない原因をさがす」の論点。過去問を暗記するための、決め手と答えの対。
   q … 問題文か提示物に一字一句ある言葉／a … 正解の選択肢に一字一句ある言葉／tip … 取り違えやすい所だけ */
(function (global) {
  "use strict";
  var P = global.POINTS = global.POINTS || {};

  /* ルータの口は 192.168.1.254、パソコンの出口は 192.168.1.1。数が違う */
  P["B1-P15-010"] = {
    q: ["192.168.1.254", "Default gateway:   192.168.1.1"],
    a: ["デフォルトゲートウェイ"],
    tip: "IP アドレスとサブネットマスクは合っている。違うのは出口の数だけ"
  };

  /* 同じネットワークとは話せて、別のネットワークとだけ話せない */
  P["B1-P15-048"] = {
    q: ["10.10.1.0/24上のユーザーには接続できません", "最初のステップ"],
    a: ["デフォルトゲートウェイに到達可能か"],
    tip: "DNS サーバーは 10.10.1.254 で、話せない側にある。先に出口まで届くかを見る"
  };

  /* 口は生きているのに線がつながっていない */
  P["B2-0178-02"] = {
    q: ["notconnect"],
    a: ["間違ったケーブルの種類"],
    tip: "shutdown なら administratively down と出る。notconnect は口は生きている印"
  };

  /* B1-P15-010 と同じ形。DNS まで書いてあるが、そちらは正しい */
  P["B2-0245-01"] = {
    q: ["192.168.1.254", "Default gateway:   192.168.1.1"],
    a: ["デフォルトゲートウェイ"],
    tip: "DNS は 8.8.8.8 で図のとおり。手で入れているので自動 DNS でもない"
  };

  /* 7km に対して、片端が 300m までの部品 */
  P["B3-M2-052"] = {
    q: ["SFP-SR", "約7KM"],
    a: ["間違ったタイプのトランシーバー"],
    tip: "-SR は 300m まで、-LR は 10km まで。ファイバーは正しいので、線ではなく端の部品"
  };

  if (typeof module !== "undefined" && module.exports) module.exports = P;
})(typeof window !== "undefined" ? window : globalThis);
