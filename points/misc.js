/* 「そのほか」の論点。**過去問を暗記するための、決め手と答えの対。**
   q … 問題文か提示物に一字一句ある言葉／a … 正解の選択肢に一字一句ある言葉／tip … 取り違えやすい所だけ */
(function (global) {
  "use strict";
  var P = global.POINTS = global.POINTS || {};

  /* ── QoS の図（右側が一列ならキューイング） ───────────── */
  P["B1-P14-016"] = {
    q: ["ホップごとのQoS動作", "一列に並んで出る"],
    a: ["キューイング"],
    why: "図の右側で、AF31・AF21・AF11 が一列に並んで出ている。これが順番待ちの列、つまりキューイング。捨てられたパケットは無いのでポリシングではない。印は AF のまま変わっていないのでマーキングでもない。",
    tip: "破棄されているならポリシング、流量をならしているならシェーピング"
  };
  P["B2-0141-01"] = {
    q: ["ホップごとの QoS 動作", "一列に並んで出る"],
    a: ["キューイング"],
    why: "三つの矢印で別々に入ってきたパケットが、出口では一列になっている。列に並んで待つ扱いなのでキューイング。数は減っていないのでポリシングではなく、出す速さをならす様子も図に無いのでシェーピングでもない。",
    tip: "順番待ちの列に並んでいるだけで、破棄はされていない"
  };

  /* ── OSPF の代表ルータ（優先度を最大にする） ───────────── */
  P["B1-P13-030"] = {
    q: ["中心点"],
    a: ["ip ospf priority 255"],
    why: "「中心点」はほかのルータが情報をまとめてやり取りする相手、つまり代表ルータのこと。代表は優先度の数が大きいほうが選ばれるので、R14 に ip ospf priority 255 を付ける。もう一方の選択肢は priority 0 で、これは代表に選ばれない設定なので逆になる。",
    tip: "優先度は 255 が最大。0 にすると代表ルータには選ばれない"
  };

  /* ── IPv6 の自動アドレス ─────────────────────── */
  P["B1-P15-069"] = {
    q: ["自動アドレス割り当て"],
    a: ["eui-64"],
    why: "自動アドレス割り当ては、アドレスの後半 64 ビットを機器が自分の MAC アドレスから作る仕組みで、コマンドの末尾に eui-64 を付ける。::1/64 は後半を手で決めているので自動ではない。anycast は複数の機器で同じアドレスを共有する別の設定。",
    tip: "後半 64 ビットは機器が自分の MAC アドレスから作るので、前半だけ書く"
  };

  /* ── 衝突が多すぎることを知らせるログ ─────────────── */
  P["B1-P16-005"] = {
    q: ["excessive collisions"],
    a: ["16回の送信試行に失敗"],
    why: "どの行の本文にも excessive collisions と書いてある。衝突が起きるたびに送り直すが、16 回続けて失敗した時点でそのフレームは破棄される決まり。短すぎるフレームや大きすぎるフレームの数え方（runts・giants）とは別の話で、バッファの過負荷とも関係しない。",
    tip: "見出しの末尾が COLL。大文字で始まる行と小文字の行が混ざるが、同じもの"
  };

  /* ── ARP を受け取ったスイッチの動き ────────────── */
  P["B2-0212-01"] = {
    q: ["初めて", "ARP を S1 に送信"],
    a: ["フラッディング", "G0/0 を除くすべてのポート"],
    why: "初めての通信なので、S1 は PC3 がどのポートの先にいるかをまだ覚えていない。行き先が分からないため、受け取った G0/0 以外のすべてのポートへ同じものを流して探す。覚えていればそのポートだけに送るが、今回は学習前。落とすこともしない。",
    tip: "宛先をまだ覚えていないので全部へ送る。受け取った G0/0 だけは除く"
  };

  /* ── 規則では答えが出せない5問。テストには本のとおりの答えで出す ──
     どこを見れば本の答えにたどり着くかを書いてある。
     showread/README.md「分かっている食い違い（本のほう）」も参照 */
  P["B1-P12-076"] = {
    q: ["Speed 100", "Speed 10"],
    a: ["速度設定が一致していません"],
    why: "速度が両側で違うとリンクそのものが上がらず、両方とも down になる。デュプレックスも Half と Full で食い違っているが、それだけならリンクは up のままで衝突が増えるだけ。両方ダウンなので原因は速度のほう。ケーブルは 99 メートルで Cat5 の 100 メートル以内、距離は問題ない。",
    tip: "デュプレックスも Half と Full で食い違うが、両方ダウンになる原因は速度のほう"
  };
  P["B1-P14-050"] = {
    q: ["Half Duplex, 1000Mbps", "line protocol is down"],
    a: ["デュプレックスの不一致"],
    why: "本の答えはデュプレックスの不一致で、根拠は 1000Mbps なのに Half Duplex と出ている点。ただしデュプレックスが食い違っても line protocol は up のままなので、down になっているこの出力とは合わない。同じ出力・同じ選択肢の問題に「ケーブルの切断」と答えている冊もある。",
    tip: "1000Mbps なのに Half。本の答えはデュプレックスの不一致だが、不一致なら line protocol は up のはずで、出力とは合わない"
  };
  P["B1-P14-072"] = {
    q: ["1790 CRC"],
    a: ["不正なチェックサム"],
    why: "本の答えは不正なチェックサムで、根拠は 1790 CRC。CRC は受け取ったフレームが途中で壊れていた数で、そのぶん捨てられている。衝突は 1 回だけなので過度の衝突ではない。ただし input errors は 0 のままで CRC の数と合っておらず、出力としてはつじつまが合わない。",
    tip: "衝突は 1 回だけなので過度の衝突ではない。ただし input errors は 0 で、本の答えは出力とは合わない"
  };
  P["B2-0084-02"] = {
    q: ["MTU 1397 bytes", "30 second input rate 0 bits/sec"],
    a: ["NIC が不良"],
    why: "ほかの3つは出力から消える。流量が 0 なので高スループットではなく、キューも落ちた数も 0 なので順番待ちでもなく、ブロードキャストは 267 だけなのでストームでもない。残るのが NIC の不良。ただし誤りも衝突も 0 で、壊れている手がかりは無い。ふつうと違うのは MTU が 1397 という点だけ。",
    tip: "流量も誤りも 0 なので、ほかの3つは消える。ふつうと違うのは MTU の 1397 だけ。本の答えは出力とは合わない"
  };
  P["B2-0251-02"] = {
    q: ["Full-duplex, 100Mb/s", "11 collisions"],
    a: ["速度とデュプレックスの不一致"],
    why: "本の答えは速度とデュプレックスの不一致で、根拠は全二重なのに 11 collisions と出ている点。全二重なら衝突は 0 のはずだから、という読み方。ただし速度が食い違えばリンクは上がらず、この出力は up のまま。落ちた数が 1 億件を超えており、同じ出力の問題に「ポートのオーバーサブスクリプション」と答えている冊もある。",
    tip: "全二重なら衝突は 0 のはず。落ちた数が桁違いに大きくオーバーサブスクリプションにも読めるが、本の答えはこちら。出力とは合わない"
  };

  if (typeof module !== "undefined" && module.exports) module.exports = P;
})(typeof window !== "undefined" ? window : globalThis);
